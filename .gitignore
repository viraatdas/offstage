.rudder/
.rudder-worktrees/
RUDDER.md
DECISIONS.md
RUDDER_SHARED.md
.claude/skills/rudder-*/

# --- offstage ---
node_modules/
dist/
.offstage/
coverage/

# OS / editor noise
.DS_Store
*.log

# ...except recorded lane fixtures. `xcodebuild.log` / `command.log` under
# tests/fixtures are checked-in evidence, not build noise: the lane parsers are
# unit-tested against them, so `*.log` above must not swallow them or the suite
# fails on a clean clone. Applies to every lane's fixtures.
!tests/fixtures/**/*.log
.rudder-workspaces/
*.tgz
