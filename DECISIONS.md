# Decisions

Shared, agent-authored log of cross-cutting decisions the fleet must honor. The conductor records plan/rebase/steer decisions here; workers record interface contracts + adjustments. Each entry is a `##` heading with **What / Why / By** so it scans. Re-read before each significant step; jj merges concurrent edits as first-class conflicts on fan-in.

## worker: Answered a question about project scope; no code changes
- **Did:** Answered a question about project scope; no code changes made. Read RUDDER.md and .rudder state: offstage is an unimplemented 11-node plan (n0 foundation -> n1-n5 parallel wave: router, headless lane, container/Xvfb lane, Tart VM lane, entitlements probe -> n6 CLI -> n7 MCP -> n8 Claude plugin/Codex wiring -> n9 e2e+live smoke -> n10 adversarial review). Repo contains only README.md, an untracked .gitignore, and Rudder scaffolding; graph.json has zero nodes so nothing was ever launched. Summarized the routing thesis, the DAG, current state, and recommended starting at n0 since it owns all manifests and the shared lane contract.
- **Interfaces:** None - no code written this turn.
- **By:** worker · 2026-08-18T00:05:41.602Z

## worker: User approved the offstage DAG. Wrote RUDDER_APPROVE_PLAN on its
- **Did:** User approved the offstage DAG. Wrote RUDDER_APPROVE_PLAN on its own line at the end of RUDDER.md (outside the generated block, plan block RUDDER_PLAN_TASKS_START/END left fully intact) per the rudder-edit-dag skill, so Rudder consumes the marker and launches the 11-node DAG. Structure: n0 foundation (owns all manifests + src/contract) -> parallel wave n1 router, n2 headless lane, n3 container/Xvfb lane, n4 Tart VM lane, n5 entitlements probe -> n6 CLI -> n7 MCP -> n8 Claude plugin/Codex wiring -> n9 e2e+live smoke -> n10 adversarial review. No source code written this turn; repo still at c5521ea with untracked .gitignore. RUDDER.md is gitignored so the marker is not a repo change.
- **Interfaces:** None - no code written this turn. The contract that all downstream nodes depend on (LaneResult/LaneRequest/Lane + zod schema in src/contract/) is n0 deliverable, not yet built.
- **By:** worker · 2026-08-18T00:56:58.228Z

## Plan approved
- **What:** Approved a 11-task plan for: Do NOT re-decompose or invent a new breakdown. The complete, user-approved 11-node DAG for offstage (n0 foundation, n1 router, n2 headless lane, n3 container lane, n4 vm lane, n5 entitlements probe, n6 CLI, n7 MCP, n8 plugin, n9 e2e, n10 review) is ALREADY written in the RUDDER_PLAN_TASKS_START/END block in RUDDER.md, with full per-node prompts, goals, success criteria and hard deps. Keep that block exactly as it is, change nothing in it, and present it for approval immediately.. Tasks: Foundation: scaffold + results contract; Router: classify a command into a lane; Headless lane: run in place, prove no window opens; Container lane: headed web under Xvfb; VM lane: adapter over novotnyllc/tart-xcode-runner; Entitlements probe: is the signing lane required?; CLI core: offstage run / route / doctor / probe; MCP server wrapping the CLI core; Claude Code plugin + skill + Codex wiring; End-to-end integration + live smoke; Opus review pass: correctness, honesty, and the routing thesis.
- **Why:** user-approved plan; workers implement these nodes in isolated workspaces, honoring the dependency edges
- **By:** conductor · 1787014935937

## worker: Launched the offstage 11-node DAG. Diagnosed why the prior
- **Did:** Launched the offstage 11-node DAG. Diagnosed why the prior RUDDER_APPROVE_PLAN marker was inert: native scan_orchestrator_markers early-returns on !awaiting_approval, and plan-queue.json had awaiting_approval=false with planned_nodes=[] because a prior session cleared the planner, orphaning the plan block in RUDDER.md. Fix required feeding two separate stores: (1) graph.json via rudder own parsePlanBlock+scaffoldPlan (NOT rudder plan, which re-decomposes with the planner model and would discard the user-approved DAG) - this also ran ensureColocated creating .jj which did not exist; (2) the plan queue, which is what the scheduler next_node_to_launch actually reads - populated by dispatching a RUDDER_PLAN skill marker (skill markers bypass orchestrator gating) whose task text directed the planner to adopt the existing block verbatim rather than re-decompose. Verified before approving: 11/11 nodes captured, ids n0-n10, deps intact, all 11 prompts verbatim with zero drift. Then wrote RUDDER_APPROVE_PLAN against a live gate. Result: n0 Foundation RUNNING, 10 nodes planned and correctly gated. Nothing pushed or deployed; main still at c5521ea.
- **Interfaces:** No product code written by me. The contract all downstream nodes depend on (src/contract/ exporting Lane, LaneRequest, LaneResult, LaneRunner + zod schema) is n0 deliverable, currently in progress. graph.json now holds 11 nodes with content-hashed ids and stacked jj change parentage matching the hard-dep DAG.
- **By:** worker · 2026-08-18T01:02:55.729Z

## n0: Foundation node complete. Clean-clone gate verified end to end:
- **Did:** Foundation node complete. Clean-clone gate verified end to end: npm ci && npm run build && npm test all pass (73 tests, 2 files), plus npm run typecheck. Created every shared manifest so no sibling ever edits one: package.json (bins offstage -> dist/cli/index.js and offstage-mcp -> dist/mcp/index.js; ALL project deps pre-installed and lockfile committed), tsconfig.json + tsconfig.build.json, vitest.config.ts, .gitignore (Rudder entries preserved verbatim, plus node_modules/dist/.offstage/coverage). Wrote src/contract/index.ts (the lane+result contract), src/contract/artifacts.ts (run dirs + result.json persistence), tests/contract.test.ts (50 tests), tests/artifacts.test.ts (23 tests), and a real README.md stating the routing thesis (offstage routes over existing substrates, it is not a new one), the three lanes, and a per-piece Status table. Empirically verified every pre-installed dependency imports AND typechecks under NodeNext ESM (plist v3 + @types/plist, fast-xml-parser, execa, commander, zod, @modelcontextprotocol/sdk) so no sibling discovers a broken import. Also verified that tests/fixtures/** is excluded from BOTH vitest collection and the TS program, by dropping in a Playwright-style fixture importing an uninstalled package and confirming test+typecheck stay green.
- **Interfaces:** src/contract/index.ts exports: type Lane ('headless'|'container'|'vm') + LANES + LaneSchema; type LaneStatus ('passed'|'failed'|'errored'|'skipped') + LANE_STATUSES + LaneStatusSchema; type ArtifactKind + ARTIFACT_KINDS + ArtifactKindSchema; interfaces LaneArtifact, LaneFailure, LaneRequest {cwd, command[], env?, timeoutMs?, artifactsDir}, LaneResult {lane,status,exitCode,startedAt,durationMs,artifactsDir,logPath,artifacts[],failures[],diagnostics[]}, LaneAvailability {available,reason?,fix?}, LaneRunner {lane, isAvailable(), run(req)}, RouteDecision {lane,reason,confidence,signals[]}; schemas LaneRequestSchema, LaneResultSchema, LaneArtifactSchema, LaneFailureSchema, LaneAvailabilitySchema, RouteDecisionSchema; helpers parseLaneResult, safeParseLaneResult, isLaneResult, parseLaneRequest, describeValidationError, isInside, statusFromExitCode, createLaneResult, skippedResult, exitCodeForResult. src/contract/artifacts.ts exports: OFFSTAGE_DIR, RUNS_DIR, RESULT_FILENAME, RunDir, makeRunId, allocateRunDir, artifactPath, toRepoRelative, writeResult, readResult, listRunIds. PATH CONVENTION (schema-enforced, documented in README + module header): LaneRequest.cwd, LaneRequest/LaneResult.artifactsDir, LaneResult.logPath and artifacts[].path are ABSOLUTE HOST paths, and logPath/artifacts[].path must be INSIDE artifactsDir; LaneResult.failures[].file is REPOSITORY-RELATIVE POSIX, relative to cwd, never absolute, no '..' escape. Use artifactPath()/toRepoRelative() to produce accepted values. LANE CONTRACT RULES: isAvailable() never throws and never mutates the world; run() never throws (return status 'errored' with diagnostics); run() never falls back to the real screen (return skippedResult(lane, artifactsDir, availability)). exitCodeForResult: passed 0, failed -> command's code or 1, errored 70, skipped 69. BUILD CONVENTIONS siblings must follow: ESM + NodeNext, so relative imports keep the .js extension; verbatimModuleSyntax is ON, so types must be imported with `import type`; vitest globals are OFF, so import describe/it/expect from 'vitest'; `npm run build` uses tsconfig.build.json (rootDir src -> dist, emits), while tsconfig.json is the noEmit typecheck/editor config that additionally covers tests/.
- **Follow-ups:**
  - Two tsconfigs by necessity: tsconfig.json (noEmit, covers src+tests) and tsconfig.build.json (rootDir src -> dist) — The node prompt asked for one tsconfig with rootDir src AND include src+tests, which tsc rejects (TS6059: tests/ is not under rootDir). Splitting is the standard resolution and keeps bin paths at dist/cli/index.js rather than dist/src/cli/index.js. No sibling needs to touch either file; `npm run build` and `npm run typecheck` both just work.
  - commander pinned to ^14 and execa to ^9 instead of latest — commander@15 requires Node >=22.12 and execa@10 requires Node >=22, which would silently break the stated Node 20+ target declared in engines. The pinned majors are current, maintained, and Node 20 compatible. Revisit only if the project raises its Node floor.
  - @types/plist kept alongside plist@^3 rather than moving to plist@^5 [out of lane] — plist@5 ships its own types, which would make @types/plist redundant and potentially conflicting. Staying on plist@3 + @types/plist@3 matches the specified dependency set and was verified to typecheck and run under NodeNext ESM. n5 (entitlements probe) should import it as `import plist from 'plist'`.
  - npm 11.16 leaves esbuild/fsevents postinstall scripts unapproved on npm ci [out of lane] — `npm ci` prints an allow-scripts warning for esbuild and fsevents. Verified harmless here: esbuild resolves its platform binary via optionalDependencies and vitest runs fine. n9 (integration/clean-clone) should confirm the same holds in whatever CI environment it targets, since a different npm config could make it fatal.
  - LaneResult path containment is enforced, not advisory [out of lane] — parseLaneResult rejects a logPath or artifact path that is not inside artifactsDir, and rejects an absolute failures[].file. This is intentional so run directories stay self-contained and failure paths stay host/guest portable, but lane authors who hand-build paths instead of using artifactPath()/toRepoRelative() will hit it. Flagging so n2/n3/n4 use the helpers rather than working around the schema.
- **By:** n0 · 2026-08-18T01:13:03.324Z

## n0: probe
- **Did:** probe
- **By:** n0 · 2026-08-18T01:13:11.940Z

## n0: Foundation node complete. Clean-clone gate verified end to end:
- **Did:** Foundation node complete. Clean-clone gate verified end to end: npm ci && npm run build && npm test all pass (73 tests, 2 files), plus npm run typecheck. Created every shared manifest so no sibling ever edits one: package.json (bins offstage -> dist/cli/index.js and offstage-mcp -> dist/mcp/index.js; ALL project deps pre-installed and lockfile committed), tsconfig.json + tsconfig.build.json, vitest.config.ts, .gitignore (Rudder entries preserved verbatim, plus node_modules/dist/.offstage/coverage). Wrote src/contract/index.ts (the lane+result contract), src/contract/artifacts.ts (run dirs + result.json persistence), tests/contract.test.ts (50 tests), tests/artifacts.test.ts (23 tests), and a real README.md stating the routing thesis (offstage routes over existing substrates, it is not a new one), the three lanes, and a per-piece Status table. Empirically verified every pre-installed dependency imports AND typechecks under NodeNext ESM (plist v3 + @types/plist, fast-xml-parser, execa, commander, zod, @modelcontextprotocol/sdk) so no sibling discovers a broken import. Also verified that tests/fixtures/** is excluded from BOTH vitest collection and the TS program, by dropping in a Playwright-style fixture importing an uninstalled package and confirming test+typecheck stay green.
- **Interfaces:** src/contract/index.ts exports: type Lane ('headless'|'container'|'vm') + LANES + LaneSchema; type LaneStatus ('passed'|'failed'|'errored'|'skipped') + LANE_STATUSES + LaneStatusSchema; type ArtifactKind + ARTIFACT_KINDS + ArtifactKindSchema; interfaces LaneArtifact, LaneFailure, LaneRequest {cwd, command[], env?, timeoutMs?, artifactsDir}, LaneResult {lane,status,exitCode,startedAt,durationMs,artifactsDir,logPath,artifacts[],failures[],diagnostics[]}, LaneAvailability {available,reason?,fix?}, LaneRunner {lane, isAvailable(), run(req)}, RouteDecision {lane,reason,confidence,signals[]}; schemas LaneRequestSchema, LaneResultSchema, LaneArtifactSchema, LaneFailureSchema, LaneAvailabilitySchema, RouteDecisionSchema; helpers parseLaneResult, safeParseLaneResult, isLaneResult, parseLaneRequest, describeValidationError, isInside, statusFromExitCode, createLaneResult, skippedResult, exitCodeForResult. src/contract/artifacts.ts exports: OFFSTAGE_DIR, RUNS_DIR, RESULT_FILENAME, RunDir, makeRunId, allocateRunDir, artifactPath, toRepoRelative, writeResult, readResult, listRunIds. PATH CONVENTION (schema-enforced, documented in README + module header): LaneRequest.cwd, LaneRequest/LaneResult.artifactsDir, LaneResult.logPath and artifacts[].path are ABSOLUTE HOST paths, and logPath/artifacts[].path must be INSIDE artifactsDir; LaneResult.failures[].file is REPOSITORY-RELATIVE POSIX, relative to cwd, never absolute, no '..' escape. Use artifactPath()/toRepoRelative() to produce accepted values. LANE CONTRACT RULES: isAvailable() never throws and never mutates the world; run() never throws (return status 'errored' with diagnostics); run() never falls back to the real screen (return skippedResult(lane, artifactsDir, availability)). exitCodeForResult: passed 0, failed -> command's code or 1, errored 70, skipped 69. BUILD CONVENTIONS siblings must follow: ESM + NodeNext, so relative imports keep the .js extension; verbatimModuleSyntax is ON, so types must be imported with `import type`; vitest globals are OFF, so import describe/it/expect from 'vitest'; `npm run build` uses tsconfig.build.json (rootDir src -> dist, emits), while tsconfig.json is the noEmit typecheck/editor config that additionally covers tests/.
- **Follow-ups:**
  - Two tsconfigs by necessity: tsconfig.json (noEmit, covers src+tests) and tsconfig.build.json (rootDir src -> dist) — The node prompt asked for one tsconfig with rootDir src AND include src+tests, which tsc rejects (TS6059: tests/ is not under rootDir). Splitting is the standard resolution and keeps bin paths at dist/cli/index.js rather than dist/src/cli/index.js. No sibling needs to touch either file; `npm run build` and `npm run typecheck` both just work.
  - commander pinned to ^14 and execa to ^9 instead of latest — commander@15 requires Node >=22.12 and execa@10 requires Node >=22, which would silently break the stated Node 20+ target declared in engines. The pinned majors are current, maintained, and Node 20 compatible. Revisit only if the project raises its Node floor.
  - @types/plist kept alongside plist@^3 rather than moving to plist@^5 [out of lane] — plist@5 ships its own types, which would make @types/plist redundant and potentially conflicting. Staying on plist@3 + @types/plist@3 matches the specified dependency set and was verified to typecheck and run under NodeNext ESM. n5 (entitlements probe) should import it as `import plist from 'plist'`.
  - npm 11.16 leaves esbuild/fsevents postinstall scripts unapproved on npm ci [out of lane] — `npm ci` prints an allow-scripts warning for esbuild and fsevents. Verified harmless here: esbuild resolves its platform binary via optionalDependencies and vitest runs fine. n9 (integration/clean-clone) should confirm the same holds in whatever CI environment it targets, since a different npm config could make it fatal.
  - LaneResult path containment is enforced, not advisory [out of lane] — parseLaneResult rejects a logPath or artifact path that is not inside artifactsDir, and rejects an absolute failures[].file. This is intentional so run directories stay self-contained and failure paths stay host/guest portable, but lane authors who hand-build paths instead of using artifactPath()/toRepoRelative() will hit it. Flagging so n2/n3/n4 use the helpers rather than working around the schema.
- **By:** n0 · 2026-08-18T01:13:40.233Z

## n0: Resolved the single 2-sided .gitignore conflict from the n0
- **Did:** Resolved the single 2-sided .gitignore conflict from the n0 foundation merge. Base had no .gitignore; the existing-code side added the 5 Rudder entries, and the n0 side contained those same 5 entries verbatim plus the offstage block (node_modules/, dist/, .offstage/, coverage/) and OS/editor noise (.DS_Store, *.log). Side B was a strict superset, so the resolution is side B unchanged — no Rudder entry altered, no offstage entry dropped. Verified the merged tree end-to-end: npm ci, npm run build (tsc -p tsconfig.build.json), npm run typecheck, and npm test all pass; 73 tests green across tests/contract.test.ts (50) and tests/artifacts.test.ts (23). jj resolve --list reports no conflicts; jj status shows .gitignore as the only working-copy change, confirming dist/ and node_modules/ are correctly ignored.
- **Interfaces:** .gitignore only — no source, manifest, or contract files touched during conflict resolution. src/contract/index.ts, src/contract/artifacts.ts, package.json, tsconfig*.json, vitest.config.ts and README.md came through the merge from the n0 side unmodified.
- **By:** n0 · 2026-08-18T01:14:39.255Z

## followup: Landed the two-tsconfig split and locked it with a
- **Did:** Landed the two-tsconfig split and locked it with a guard test. Discovered n0 had already written both files, so I made my copies BYTE-IDENTICAL to the now-merged main checkout (verified by diff after n0 went merged-locally) — my integration cannot conflict. The net-new value is tests/tsconfig.test.ts (13 tests), which encodes WHY the split exists so a future 'simplify to one tsconfig' fails loudly instead of shipping a broken dist layout. Empirically proved both halves of the necessity argument rather than restating them: (A) one config with rootDir=src + include src&tests dies with TS6059 on every tests/ file; (B) relaxing rootDir to '.' compiles but emits dist/src/contract/index.js AND dist/tests/*.test.js, invalidating package.json bin (dist/cli/index.js), exports (./dist/contract/index.js) and types. Verified the division of labor directly: injected a type error into tests/ and confirmed `tsc -p tsconfig.json` catches it (exit 1) while `tsc -p tsconfig.build.json` stays green (exit 0) and emits zero test files; a colocated src/**/*.test.ts is also excluded from the build. Mutation-tested the guard three ways (build rootDir->'.', typecheck drops tests/, typecheck noEmit->false) and confirmed it fails on each, so it is not vacuous. Final gate on a simulated post-integration tree (n0 foundation + my files): npm run typecheck, npm run build and npm test all pass, 86 tests / 3 files (73 n0 + 13 mine), dist is exactly dist/contract/{index,artifacts}.{js,d.ts} with no dist/src leak, and node actually imports ./dist/contract/index.js and reads LANES.
- **Interfaces:** Files I own: tsconfig.json (noEmit typecheck/editor config; include src+tests+scripts+vitest.config.ts; excludes tests/fixtures) and tsconfig.build.json (extends ./tsconfig.json, rootDir src -> outDir dist, noEmit false, include src only, excludes src/**/*.test.ts). Both byte-identical to n0's merged versions, so no sibling needs to touch them. New file: tests/tsconfig.test.ts — reads both configs through the real TypeScript API (ts.readConfigFile + ts.parseJsonConfigFileContent, so JSONC and `extends` resolve exactly as tsc sees them) and asserts the effective options, not the raw JSON. Assumes only devDeps n0 already installed (typescript, vitest); no manifest change. Invariants now enforced by test: tsconfig.json noEmit===true and its program covers both src/ and tests/ while excluding tests/fixtures; tsconfig.build.json extends './tsconfig.json', noEmit===false, rootDir===<root>/src, outDir===<root>/dist, declaration===true, program contains only src/ files and zero *.test.ts; tests/ resolves outside the build rootDir (the TS6059 condition); every dist path declared in package.json bin/exports/types matches ^dist/ and never contains dist/src/.
- **Follow-ups:**
  - n6/n7 should re-run the guard once dist/cli/index.js and dist/mcp/index.js exist [out of lane] — tests/tsconfig.test.ts asserts the SHAPE of the published paths in package.json (under dist/, never dist/src/), not that the files exist, because the CLI and MCP entry points are not built yet. Once n6 and n7 land, `npm run build` should be confirmed to actually emit dist/cli/index.js and dist/mcp/index.js at those exact paths — that is the moment the bin declarations become checkable end to end.
  - tsconfig.json still carries rootDir '.' and outDir 'dist' under noEmit — Those two options are inert while noEmit is true, and tsconfig.build.json overrides both, so nothing is broken today. Left as-is deliberately to stay byte-identical to n0's merged file and avoid a pointless integration conflict. Flagging only so a reviewer does not mistake them for the emit settings — the emitting config is tsconfig.build.json.
  - include lists scripts/**/*.ts, a directory that does not exist yet — Harmless (tsc tolerates a glob matching nothing as long as some include matches), and it means any future build/release script is typechecked the moment it is added without editing the config. Noting it so nobody 'fixes' the apparently-dead glob and silently drops future scripts out of the typecheck program.
- **By:** followup · 2026-08-18T01:18:19.605Z

## followup-2: Verified the commander ^14 / execa ^9 pins against
- **Did:** Verified the commander ^14 / execa ^9 pins against the live npm registry and locked them in with a regression guard. The pins themselves were already implemented by n0 and survived its conflict resolution on merged main (534ab2cd): package.json has commander ^14.0.3 + execa ^9.6.1, lockfile resolves 14.0.3 / 9.6.1. Confirmed every premise of the decision empirically rather than trusting it: commander dist-tag latest is 15.0.0 with engines node >=22.12.0, execa latest is 10.0.1 with engines node >=22 -- both would break the declared engines.node >=20, and npm does not fail an install on unsatisfied engines, so taking latest would leave offstage installable but broken on Node 20. The pinned majors are the newest Node-20-compatible ones (commander 14.0.3 engines >=20; execa 9.6.1 engines ^18.19.0 || >=20.5.0), and both import and execute under NodeNext ESM. I did NOT edit package.json: DECISIONS.md records it as an n0-owned shared manifest that no sibling edits, and my workspace branches off the root commit, so writing one would have forced an add/add conflict into an already-conflicted integration. Added tests/dependency-pins.test.ts (10 tests) instead -- the only new file in my diff. It asserts the caret ranges are ^14.x/^9.x (not latest/*/>=), that the lockfile resolves to those majors, that neither locked package carries a Node 22 floor, that the manifest still advertises Node 20, and that no dep anywhere uses a floating tag.
- **Interfaces:** NEW FILE tests/dependency-pins.test.ts -- standalone vitest file, no imports from src/, reads only package.json and package-lock.json via new URL(..., import.meta.url). Picked up by the existing vitest include tests/**/*.test.ts and by tsconfig.json's typecheck; needs no config change. Assumes repo-root package.json + package-lock.json exist one level above tests/ (true on merged main). Internal helper minNodeMajor() parses only the >=x / ^x / 'a || b' engines shapes and throws on anything else, so an unrecognised range fails loudly instead of passing silently.
- **Follow-ups:**
  - engines.node ">=20" is looser than execa@9's own floor of >=20.5.0 [out of lane] — package.json advertises node >=20, but execa 9.6.1 declares ^18.19.0 || >=20.5.0. Node 20.0-20.4 therefore satisfies offstage's stated floor while violating a runtime dep's -- the same silent-breakage class the commander/execa pins exist to prevent, just narrower. Tightening engines.node to >=20.5.0 closes it. I left it alone deliberately: package.json is n0-owned, and my guard test names the gap in a comment rather than asserting a claim it cannot prove (it checks 'no Node 22 floor', not 'runs on every 20.x').
  - Revisit both pins when the Node floor is raised [out of lane] — commander 15 and execa 10 are held back only by engines.node >=20. If offstage ever moves to Node 22+, both pins should be lifted together and tests/dependency-pins.test.ts updated -- its PINS table and NODE_FLOOR_MAJOR constant are the single place to edit.
- **By:** followup-2 · 2026-08-18T01:18:44.047Z

## followup-3: Removed the dead emit paths from tsconfig.json: rootDir "."
- **Did:** Removed the dead emit paths from tsconfig.json: rootDir "." and outDir "dist" were declared under noEmit:true, where tsc ignores them, and tsconfig.build.json overrides both anyway (rootDir src -> outDir dist). Replaced them with a comment above noEmit explaining that this config declares no emit paths on purpose, that the published layout lives in tsconfig.build.json and only there (which is what keeps bin/exports at dist/cli/index.js rather than dist/src/cli/index.js), that build output is kept out of the typecheck program by "dist" in exclude rather than by outDir, and that declaration/declarationMap/sourceMap deliberately stay because tsconfig.build.json extends this file and does emit. Behavior is unchanged, verified by comparing resolved configs: tsc --showConfig -p tsconfig.json now reports rootDir/outDir absent with noEmit true and the same file set (2 src, 2 tests, 0 fixtures), while tsc --showConfig -p tsconfig.build.json still reports rootDir ./src, outDir ./dist, noEmit false, declaration true over exactly src/contract/{index,artifacts}.ts. Gates: npm run typecheck PASS, npm run build PASS emitting dist/contract/index.js (flat under dist, never dist/src/), npm test 73/73 PASS. Also temporarily copied the already-merged tests/tsconfig.test.ts guard from the main checkout and ran it against this change: 86/86 passed including its assertions that the build config resolves rootDir src / outDir dist / declaration true and that no package.json published path leaks dist/src/; the temp copy was then deleted since that file belongs to the followup node and is already in main.
- **Interfaces:** Files I own: tsconfig.json only (one-file diff, verified via jj diff). No exports, types, or scripts changed. Contract for siblings is unchanged: npm run build still uses tsconfig.build.json (rootDir src -> dist, emits declarations), npm run typecheck still uses tsconfig.json (noEmit, covers src + tests + scripts + vitest.config.ts, excludes tests/fixtures). Nothing reads tsconfig.json rootDir/outDir. Kept the diff surgical to avoid conflicting with the sibling node editing the include list in the same file.
- **Follow-ups:**
  - Add two assertions to tests/tsconfig.test.ts that tsconfig.json declares no rootDir and no outDir [out of lane] — The existing guard test only pins the build config, so re-adding dead emit paths to tsconfig.json would not fail anything. I did not add them myself: tests/tsconfig.test.ts is the followup node file and is already merged in main but absent from my worktree base, so writing it here would have produced an add/add conflict for a regression that is a clarity issue rather than a behavior one.
- **By:** followup-3 · 2026-08-18T01:21:10.191Z

