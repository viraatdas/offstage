{
  "mcpServers": {
    "offstage": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@viraatdas/offstage@latest", "offstage-mcp"],
      "note": "Runs the published package, so this works from a plain plugin install — which clones without running npm install or a build — and from any project. Pin a version by replacing @latest. To run a local checkout instead: claude mcp add offstage -- node /absolute/path/to/offstage/dist/mcp/index.js"
    }
  }
}
