{
  "mcpServers": {
    "offstage": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/mcp/index.js"],
      "note": "Exposes offstage_doctor / offstage_route / offstage_run / offstage_probe. Requires `npm ci && npm run build` in the plugin root once: dist/ is a build output and is not committed."
    }
  }
}
